# Development Roadmap

开发严格按 `Plan.md` 的阶段推进。

## 当前阶段

阶段 1：底图导入与坐标校准。

验收重点：

- PNG 底图可以导入并在图纸区缩放、拖拽查看。
- 左键标记两个参考点，右键或中键拖拽平移，滚轮缩放。
- 参考点保存为底图像素坐标，不保存屏幕坐标或视图临时状态。
- 在可靠 X/Y 方向假设下，两点校准分别推导 `scaleX`、`scaleY`。
- 底部状态栏实时显示鼠标 CAD 坐标。

## 下一阶段

阶段 2：拓扑网络绘制。
